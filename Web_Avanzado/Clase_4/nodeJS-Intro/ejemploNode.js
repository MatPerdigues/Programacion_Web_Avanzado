const sumar = (num1,num2)=>{
    return num1+num2;
}

const SALUDO = (nombre)=>{
    console.log("Hola " + nombre);
}

module.exports={sumar,SALUDO}; //importante poner el export al final del archivo.